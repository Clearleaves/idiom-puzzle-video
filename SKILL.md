---
name: idiom-puzzle-video
description: 制作看图猜成语、谐音猜谜的竖屏短视频：审题、生成原创立体谜面、加入倒计时与提示、片尾揭晓答案，导出带音乐的 MP4 和封面。适用于“做一条猜成语视频”“换个成语再做一期”等请求。
---

# 看图猜成语视频

把一个成语做成可播放、可下载的短视频，不只交付提示词或静态图片。默认沿用暖米色背景、金色立体字、写实静物的统一栏目风格。

## 默认成片

- 1080×1920，9:16，24 fps，H.264/AAC MP4，16 秒。
- 前 11 秒显示四个线索、倒计时和轻微镜头推进；中途给提示；后 5 秒揭晓答案与谐音解释。
- 原创合成轻音乐与倒计时提示音，默认没有口播。
- 单独导出封面。以上都是可修改的默认值，不是平台流量公式。

用户只说“换个成语”时，保留上一期风格和节奏，选一个未用过、线索自然的四字成语，期数递增。无需反复确认普通制作细节。

## 1. 先审题

先写出成语、从左到右的四个线索、每个线索对应的字，以及谐音解释，再生成图片。优先常见成语、可辨认的日常物品，不为凑四个物品强行编造答案。

- 谐音可允许声调不同，并明确这是趣味谐音谜题；不要声称是严格同音或标准语文教学。
- “犬→全”“梅→美”这类转换可以解释。若使用物品名称的一部分，明确提示取字规则。
- 排查物品歧义、数量、顺序，以及是否存在同样合理的另一答案。
- 用户指定的成语不适合四个物体时，先尝试更自然的文字与物品组合；仍不成立则提出具体替代题型，不能悄悄换掉用户指定的答案。

配置结构及两个已制作过的案例见 [references/config.md](references/config.md)。样例在 `assets/`，批量制作时每期单独配置，不覆盖上一期。

## 2. 生成原创谜面

使用可用的内置 `image_gen` 工具；若环境提供 imagegen 技能，遵循其工具调用规范。读取 [references/image-prompt.md](references/image-prompt.md)，替换四个线索生成背景图。

- 不把用户截图中的界面、弹幕、昵称或“上期答案”复制进画面。
- 标题、提示、解释由脚本排版；图中只生成作为谜面一部分的立体文字。
- 先查看生成图，确认四个线索完整、顺序正确，没有多余物品或文字，顶部与底部留白足够。生成缺陷使用图像工具修正，脚本仅负责视频合成。
- 不把整段 base64 输出到日志。读取工具返回的保存路径，将素材复制到本次工作区。保存最终生成提示词到工作区，方便下一期延续风格。
- 图像工具不可用时，说明阻塞并使用用户提供的适用素材；不能假装生成成功或偷偷切换付费 API。

## 3. 合成视频

复用 [scripts/render_video.py](scripts/render_video.py)，无需重新编写渲染器。脚本接受独立图片和 JSON 配置，不依赖创建本技能的会话或历史文件。

倒计时数字和答案占位问号使用 `text_in_box`：根据 `textbbox` 的实际字形边界，在圆形或方框内水平、垂直居中，再给数字下移 6 px、问号下移 8 px 的小幅光学校正。不要用固定顶部坐标、字号的一半或字体行高中线代替字形居中；更换字体、字号或容器大小后仍按实际边界计算，光学校正也要随字号按比例调整。

依赖为 Python、Pillow、NumPy、FFmpeg（系统安装或 `imageio-ffmpeg` 提供），以及可显示中文的字体。依赖清单为 [scripts/requirements.txt](scripts/requirements.txt)。如果需要安装，优先放到任务工作区虚拟环境或 `work/deps`，不要修改技能目录或全局 Python。Windows 默认探测微软雅黑；其他系统通过 `--font`、`--font-bold` 指定字体。

```text
python <skill-dir>/scripts/render_video.py --image <work-dir>/puzzle.png --config <work-dir>/episode.json --output-dir <outputs-dir> --work-dir <work-dir>/render
```

支持 `--font`、`--font-bold`、`--ffmpeg`、`--overwrite`。默认拒绝覆盖已有成片；需要新版本时修改配置中的 `filename`。脚本生成 MP4、JPG 封面、JSON 技术检查报告，抽查帧与音频留在工作目录。图片、输出目录和工作目录不要放在已安装的技能目录内。

如依赖安装到 `work/deps`，为本次 Python 子进程设置对应 `PYTHONPATH`。用户需要口播、其他画幅、复杂动画或外部配乐时，在默认脚本之外按需求扩展；不要把脚本当前能力描述成已经支持这些功能。默认音乐不需要联网或第三方歌曲。

## 4. 检查并交付

脚本会验证配置、解码完整视频并写入检查报告；这不能替代目视检查。查看工作目录中的开场、中段和答案帧，核对：

- 字体不是方框，标题、提示和答案不溢出；四个线索不被字幕或答案卡遮住。
- 检查单、双位倒计时数字和四个问号在各自容器中居中，上下留白基本一致。
- 倒计时结束才揭晓；解释对应当前成语，没有残留上一期答案。
- MP4 可解码、音轨存在且不是全静音；时长和画幅符合请求。

有预览工具时播放或打开成片；至少检查代表帧和解码结果。不声称已听过没有实际播放的声音。

最终直接展示视频并提供成片、封面链接，简要说明时长与是否有口播。用户要制作视频不等于授权发布账号，默认只交付文件。画面标注“AI 生成画面”不代替平台发布时的 AI 内容声明；如用户要求发布，再按平台当时的实际要求处理。
